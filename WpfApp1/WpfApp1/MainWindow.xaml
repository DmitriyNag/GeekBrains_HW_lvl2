<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d"
        Title="MainWindow" Height="700" Width="1200">
    <Grid Name ="MainGrind">
        <Button Name ="BtnAdd" Content ="Add" HorizontalAlignment="Left" Margin="1000,75,0,0" VerticalAlignment="Top" Height="30" Width="150" Click="BtnAdd_Click"/>
        <Button Name ="BtnSort" Content="Sort by my own sorting" HorizontalAlignment="Left" Margin="1000,150,0,0" VerticalAlignment="Top" Height="30" Width="150" Click="BtnSort_Click"/>
        <ListBox Name ="DataList" Margin="0,0,200,0" FontSize="7">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Rectangle Height="1"  Width="{Binding Path=Width}">
                    <Rectangle.Fill>
                            <SolidColorBrush Color="{Binding Path=Color}" />
                    </Rectangle.Fill>
                    </Rectangle>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
